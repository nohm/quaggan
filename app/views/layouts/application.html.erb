<!DOCTYPE html>
<html>
  <head>
    <meta charset='utf-8'>
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Quaggan" %>">
    <meta name="copyright" content="<%= ENV["DEV_NAME"] %>">
    <meta name="msapplication-TileImage" content="<%= image_path('logo.png') %>" />
    <meta name="msapplication-TileColor" content="#232323" />

    <title><%= content_for?(:title) ? yield(:title) : "Quaggan" %></title>
    
    <link href='https://fonts.googleapis.com/css?family=Open+Sans:400' rel='stylesheet' type='text/css'>

    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
    <%= yield(:head) %>
  </head>
  <body data-controller="<%= controller_name %>" data-action="<%= action_name%>" id="<%= controller_name %>" class="<%= action_name %>">

    <div id="main" role="main">
      <div class="container">
        <div class="content">
          <div class="row">
            <div class="col-md-4 col-md-offset-4">
              <div class="header">
                <h1>Quaggan</h1>
              </div>

              <%= render 'layouts/messages' %>

              <%= yield %>
            </div>
          </div>
        </div>
      </div>
    </div>

    <%= render 'layouts/footer' %>

  </body>
</html>
